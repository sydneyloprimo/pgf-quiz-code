export enum Cookies {
  authIdToken = 'auth_id_token',
  authPkce = 'auth_pkce',
  authRefresh = 'auth_refresh',
  cart = 'cart',
  customerAccessToken = 'token',
}

export enum CartCookies {
  cartId = 'cartId',
}
