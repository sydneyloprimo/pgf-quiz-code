export enum Events {
  viewVariant = 'view_variant',
  signUp = 'sign_up',
  login = 'login',
  removeFromCart = 'remove_from_cart',
  addProduct = 'add_product',
  goCheckout = 'go_checkout',
}

export enum AuthenticationMethods {
  email = 'email',
  google = 'google',
  facebook = 'facebook',
}
