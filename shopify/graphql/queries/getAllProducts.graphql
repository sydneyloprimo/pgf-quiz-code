query getAllProducts(
  $first: Int = 50
  $query: String = ""
  $sortKey: ProductSortKeys = RELEVANCE
  $reverse: Boolean = false
  $after: String = null
  $before: String = null
) {
  products(
    first: $first
    sortKey: $sortKey
    reverse: $reverse
    query: $query
    after: $after
    before: $before
  ) {
    ... on ProductConnection {
      pageInfo {
        hasNextPage
        hasPreviousPage
        endCursor
        startCursor
      }
      edges {
        cursor
        node {
          id
          title
          vendor
          handle
          priceRange {
            minVariantPrice {
              amount
              currencyCode
            }
          }
          images(first: 1) {
            pageInfo {
              hasNextPage
              hasPreviousPage
            }
            edges {
              node {
                url
                altText
                width
                height
              }
            }
          }
        }
      }
    }
  }
}
