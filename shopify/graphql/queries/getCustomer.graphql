query getCustomer($customerAccessToken: String!) {
  customer(customerAccessToken: $customerAccessToken) {
    id
    firstName
    acceptsMarketing
    email
    phone
    createdAt
    defaultAddress {
      address1
      city
      country
      address2
      formattedArea
      zip
    }
  }
}
