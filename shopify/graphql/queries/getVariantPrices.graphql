query getVariantPrices($variantIds: [ID!]!) {
  nodes(ids: $variantIds) {
    ... on ProductVariant {
      id
      price {
        amount
        currencyCode
      }
      product {
        title
      }
    }
  }
}
