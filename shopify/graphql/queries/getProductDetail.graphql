query GetProductDetail($handle: String!) {
  product(handle: $handle) {
    id
    title
    vendor
    handle
    description
    availableForSale
    priceRange {
      minVariantPrice {
        amount
        currencyCode
      }
      maxVariantPrice {
        amount
        currencyCode
      }
    }
    images(first: 1) {
      edges {
        node {
          url: url(transform: { maxWidth: 600, crop: CENTER })
          width
          height
          altText
        }
      }
    }
    variants(first: 50) {
      edges {
        node {
          id
          title
          image {
            url: url(transform: { maxWidth: 600, crop: CENTER })
            width
            height
            altText
          }
          availableForSale
          quantityAvailable
          price {
            amount
            currencyCode
          }
          selectedOptions {
            name
            value
          }
        }
      }
    }
  }
}
